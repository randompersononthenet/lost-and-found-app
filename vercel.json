{
  "version": 2,
  "buildCommand": "cd admin && npm install && npm run vercel-build",
  "outputDirectory": "admin/dist",
  "routes": [
    {
      "src": "/admin/(.*)",
      "dest": "/admin/$1"
    },
    {
      "src": "/(.*)",
      "dest": "/admin/$1"
    }
  ]
}
