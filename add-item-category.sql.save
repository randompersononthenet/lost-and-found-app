-- Add item_category to posts for finer-grained categorization (e.g., Electronics, Wallets)
ALTER TABLE posts
  ADD COLUMN IF NOT EXISTS item_category TEXT;

-- Optional: add a CHECK constraint to restrict to known categories
-- ALTER TABLE posts
--   ADD CONSTRAINT chk_posts_item_category
--   CHECK (item_category IN ('Electronics','Wallets','Keys','Documents','Clothing','Bags','Accessories','IDs','Others'));2

-- Index to speed up searches by item_category
CREATE INDEX IF NOT EXISTS idx_posts_item_category ON posts(item_category); 
